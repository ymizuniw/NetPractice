*This project has been created as part of the 42 curriculum by ymizuniw*

#[Description]

#[Instruction]
<!--
The Instructions section must explain how to run the training interface (e.g., open
index.html), how to export configurations, and submission requirements.-->
1. open chrome browser on your device.
2. execute ./net_practice/run.sh in repository root, and click "net_practice" in the index shown in the opened browser.
3. login to Training/Evaluation interface on the browser depending on your purpose.
4. when you finish one configration, then click "Get my config" bottun on the interface to save the configration to local.
5. confirm that all 10 level configuration files on the repository root. This is the requirement of this subject.


#[Resources]
<!--
The Resources section must explicitly mention the networking concepts studied,
such as TCP/IP addressing, subnet mask, default gateway, routers and
switches, OSI layers, etc.-->

#[Requirements]
<!--
    Submission details must state that 10 exported configuration files (one per level  c  )
must be placed at the repository root.
-->
##[level1]
Two local network exists, and they have corresponding IP and network address

##[level2]
Two local network exists, and they have corresponding IP and network address.

##[level3]
One network consists of three hosts connected by Switch_1, and they have the same network address.

##[level4]
One network consists of GatewayR1, and two hosts connected with Switch_1. Notice that the three gateway IP and masks are similar and should be discriminated each other.

##[level5]
Two different networks those are from router R, and Routing of each hosts(hostB, hostA) are routed to each network.

##[level6]
One local network consists from the gateway interfaceR1, and Internet is connected with gateway's interface R2. Routing table along with the packet flow is the point.

##[level7]
Local network with two routers between two hosts.

##[level8]
The destination network address is restricted to 157.89.189.0/26 on internet 1, then R21, R22, R23 have the same network address.Also, three networks around router R2 is discriminated from each other by the mask of /28 (for head four bits of last octet : 0011->48-63, 0010->32-47, 0001->17-31).

##[level9]


##[level10]
